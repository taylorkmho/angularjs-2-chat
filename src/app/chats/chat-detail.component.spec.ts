import {
  async,
  inject,
  addProviders
} from '@angular/core/testing';

import { ChatDetailComponent } from './chat-detail.component';

describe('Chat Detail Component', () => {
  beforeEach(() => {
    addProviders([ChatDetailComponent]);
  });



});
